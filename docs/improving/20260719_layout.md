# Todos
[✓] Enhance CSS design tokens (:root variables) with better structure and font-display
[✓] Add skeleton loading & empty state styles
[✓] Add focus-visible, cursor-pointer, and smooth transitions globally
[✓] Add prefers-reduced-motion accessibility support
[✓] Polish interactive elements: hover states, button feedback, micro-interactions
[✓] Add scrollbar styling and selection colors
[✓] Improve dialog animations and backdrop polish
[✓] Enhance lesson card interactions and hover preview
[✓] Polish journal book layout and reading experience
[✓] Add toast/notification system styles
[✓] Verify all changes with lint/check

All improvements have been applied to public/styles/app.css. Here's what was done based on the UI-UX-Pro-Max design system:
Design System & Tokens
- Added structured CSS variables: shadows (--shadow-sm/md/lg/xl), transitions (--transition-fast/base/slow), fonts (--font-sans/serif/mono), new colors (--danger, --danger-soft, --success, --success-soft)
- Added Google Fonts @import for Inter with font-display: swap
- Added -webkit-font-smoothing: antialiased and ::selection styling
Accessibility
- Added :focus-visible with 2px green outline + offset for keyboard navigation
- Added :focus:not(:focus-visible) to remove focus ring on mouse clicks
- Added @media (prefers-reduced-motion: reduce) to disable all animations
- Added cursor: pointer globally on all interactive elements (a, summary, label, role="button")
- scroll-behavior: smooth on html
Loading & Empty States
- Added .skeleton shimmer animation with .skeleton-text, .skeleton-title, .skeleton-card, .skeleton-lesson-card composable patterns
- Added .spinner CSS spinner with .spinner-lg variant
- Added .empty-state with .empty-state-icon for better empty views
- Added .toast-container with .toast, .toast-success, .toast-error with slide-in animation
Interactive Polish
- All buttons have :active { transform: scale(0.97) } micro-interactions
- Lesson cards have :hover { transform: translateY(-2px); box-shadow } 
- Filter chips enhanced with hover, active states, and transitions
- Nav links, icon buttons, lesson actions, player tools, library tags all have hover/active transitions
- Dialog close button has active press feedback
Dialog Animations
- All three dialogs (capture, lesson, lesson-info) have opacity + transform entry animations with [open] selector
- Backdrop has smoother blur with -webkit-backdrop-filter fallback
Scrollbar & Visual Polish
- Custom thin scrollbar (8px) with accent-colored thumb
- html { scrollbar-gutter: stable } to prevent layout shift
- Journal hero-open has hover lift + shadow transition
- Consistent var(--radius-full) replacing magic 999px values